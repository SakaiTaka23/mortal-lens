{
  "name": "mortal-lens",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "types": "pnpm -F \"types\"",
    "parser": "pnpm -F \"parser\"",
    "processor": "pnpm -F \"processor\"",
    "test": "vitest",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "format": "pnpm -r format",
    "format:fix": "pnpm -r format:fix",
    "check": "pnpm build && pnpm lint && pnpm format && vitest run"
  },
  "packageManager": "pnpm@10.6.4",
  "dependencies": {
    "@mortal-lens/eslint-config": "workspace:^",
    "@mortal-lens/prettier-config": "workspace:^",
    "@mortal-lens/tsconfig-config": "workspace:^",
    "eslint": "^9.26.0",
    "prettier": "^3.5.3",
    "tsup": "^8.4.0",
    "vitest": "^3.1.3"
  }
}
